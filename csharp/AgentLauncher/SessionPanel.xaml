<Window x:Class="AgentLauncher.SessionPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Session Monitor"
        Width="420" Height="320"
        MinHeight="80"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Opacity="0.92"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Border Background="#1E1E2E"
            CornerRadius="18"
            BorderBrush="#45475A"
            BorderThickness="1"
            ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header (top corners only) -->
            <Border Grid.Row="0" Background="#2E1A47"
                    CornerRadius="18,18,0,0" Height="28"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown"
                    Cursor="Hand">
                <Grid>
                    <Border Height="1" Background="#CBA6F7"
                            VerticalAlignment="Bottom"/>
                    <TextBlock Text="Session Monitor"
                               Foreground="#CDD6F4"
                               FontFamily="Segoe UI" FontSize="11" FontWeight="Bold"
                               VerticalAlignment="Center" Margin="12,0,0,2"/>
                    <TextBlock x:Name="ClockLabel"
                               Text="--:--:--"
                               Foreground="#A6ADC8"
                               FontFamily="Consolas" FontSize="10"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Margin="0,0,14,2"/>
                </Grid>
            </Border>

            <!-- Body (bottom corners) -->
            <Border Grid.Row="1" CornerRadius="0,0,17,17"
                    Background="#1E1E2E" ClipToBounds="True">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Hidden"
                              Background="Transparent">
                    <StackPanel x:Name="BodyPanel" Margin="6,6,6,8"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>
